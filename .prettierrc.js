module.exports = {
	tabWidth: 4,
	useTabs: true, // use tabs instead of spaces.
	printWidth: 200, // line wraps after 200 length
	trailingComma: 'es5',
	semi: true, // add semicolons;
	singleQuote: true, // use single quotes.
	quoteProps: 'preserve', // if one object property requires quotes, add quotes to all of them.
	jsxSingleQuote: false, // Use single quotes instead of double quotes in JSX.
	trailingComma: 'all', // Print trailing commas wherever possible when multi-line
	bracketSpacing: true, // Print spaces between brackets in object literals.
	jsxBracketSameLine: false,
	arrowParens: 'avoid',
	endOfLine: 'lf',
};
